/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
.DS_Store

# Scratch outputs from the sync task. The committed artefacts live
# under priv/static/ and priv/grammar/; local/temporary files must
# not leak into the package.
/tmp/

# Node-based JS integration tests live under test/js/ and pull npm
# devDeps on first run. The lockfile is committed; node_modules is not.
/test/js/node_modules/
